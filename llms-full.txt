# The Hallucinated Lab — Full Site Directory

> Expanded machine-readable map of https://thehallucinatedlab.space. Every page, every section, and what each one contains. Written so a crawler that reads only this file still has an accurate model of the site.

Last updated: 2026-07-30
Canonical host: https://thehallucinatedlab.space
Stack: static HTML, CSS, and vanilla JavaScript. No build step, no framework, no analytics, no cookies. Hosted on GitHub Pages.

---

## What The Hallucinated Lab is

The Hallucinated Lab is a local-first AI lab founded by Pratyush, Divyansh Tripathi and Shashwat Deep. Pratyush and Divyansh are computer science undergraduates at Manipal University Jaipur, India.

The lab has one governing constraint: if a model can run on the user's hardware, the user should be able to run it without a subscription, a rate limit, or a third party reading their data. Every tool the lab publishes runs on the visitor's own machine.

The lab does three distinct things:

1. It publishes free artifacts — prompts, LoRA adapters, open-source packages, interactive explainers, and a local-model chat interface.
2. It sells AI consulting and engineering services to individuals and businesses.

The Hallucinated Lab is not a SaaS company. There is no hosted product, no subscription, no usage-based pricing, and no customer data stored on any server operated by the lab.

---

## Navigation structure

The top-level navigation has seven entries in this order: Home, Tools, Pipelines, Assistant, Solutions, Media, Sitemap.

Tools and Pipelines are the two halves of the same toolkit: a tool does one thing, a pipeline
chains several tools in order. Both are exposed by the `thl` command under matching namespaces,
`thl tool <name>` and `thl pipeline <name>`.

Three of these are gateways to deeper pages:
- Pipelines documents the RAG and EDA pipelines, and links onward to /eda.html.
- Media links onward to Blogs at /blogs.html and Artifacts at /artifacts.html.

---

## Page: Home — https://thehallucinatedlab.space/

Purpose: orient a first-time visitor and explain what lives behind every tab.

Sections:
- Hero. States the lab's position: open-source AI tools running entirely on the visitor's machine, free, and without any rate limits.
- Explore the Lab. Six cards, one per navigation tab, each summarising what that section contains and who it is for.
- About Us. Profiles of the two founders with links, followed by contact details.

---

## Page: Tools — https://thehallucinatedlab.space/tools.html

Purpose: everything the lab publishes that is meant to be taken away and run elsewhere, as one flat grid of cards — one card per tool.

Cards: Convert, Extract, Chunk, Tokenize, Embed, Index, Prompts, LoRA Adapters. Each links to its own page. Every tool card corresponds to a `thl tool <name>` command; the pipelines that chain them live at /pipelines.html.

### Section 1 — Prompts (/tools.html#prompts)

Eight model-agnostic prompts, each free to copy with no account. They work on any instruction-tuned model, local or hosted. Filterable by category.

1. Adversarial Code Reviewer (Engineering). Forces ranked, evidence-backed review findings with a concrete failure case attached to each one. Drops any finding that cannot be demonstrated.
2. Root-Cause Debugger (Engineering). Runs a differential diagnosis: restate the failure, rank hypotheses by prior probability, propose the cheapest discriminating experiment for each, then stop before proposing a fix.
3. Refactor Without Rewrites (Engineering). Behaviour-preserving refactoring under a strict contract — no new dependencies, no renamed public symbols, every change justified, approval required before code is emitted.
4. Dataset Triage (Data). Audits a schema and sample rows for target leakage, integrity failures, distribution risk, and correct split strategy before any modelling starts.
5. Plain English to SQL (Data). Schema-grounded query generation that refuses to invent columns and surfaces ambiguity instead of guessing. Returns the query, an explanation per CTE, and the index that fixes it at scale.
6. Socratic Tutor (Learning). Diagnoses the learner's current understanding, addresses one gap per turn, and tests with problems that require understanding rather than recall.
7. Paper Distiller (Writing). Reviews a paper as a skeptic: separates the claim from the evidence, names the gap between them, identifies genuine novelty versus rebranding, and reports what is missing for reproduction.
8. Local Model Router (Agents). System prompt for a small routing model that classifies each request and emits strict JSON directing it to a small model, a large model, a code model, retrieval, or refusal.

### Section 2 — LoRA Adapters (/tools.html#adapters)

Small fine-tuned adapters that stack on top of a base model the user already runs locally. Tens of megabytes each rather than a new multi-gigabyte model download, and unloadable at will.

Status: none are downloadable yet. All four are in training or queued. Each card will publish weights, an evaluation card, and a licence when it clears its benchmark.

- thl-review-8b. Base Llama 3.1 8B Instruct, rank 16 alpha 32, attention projections, approximately 42 MB. Produces structured code review output instead of prose commentary.
- thl-sql-3b. Base Qwen 2.5 3B Instruct, rank 8 alpha 16, attention and MLP, approximately 18 MB. Schema-grounded SQL that refuses rather than hallucinating a column.
- thl-router-1b. Base Llama 3.2 1B Instruct, rank 8 alpha 16, query and value projections, approximately 9 MB. Strict-JSON request classifier for routing.
- thl-notes-7b. Base Mistral 7B Instruct v0.3, rank 16 alpha 32, attention, approximately 38 MB. Turns transcripts into structured notes with claims, open questions, and action items kept separate.

The section also publishes working usage guides for two runtimes: an Ollama Modelfile with an ADAPTER directive, and a Python example loading an adapter through PEFT and hot-swapping between adapters without reloading the base model.

---

## Page: Prompts — https://thehallucinatedlab.space/prompts.html

Purpose: a library of eight prompts written for real engineering tasks, free to copy with no account.

- Model-agnostic. Each is written to run on whatever model the reader already has locally, not against a specific hosted API.
- The eight: Adversarial Code Reviewer, Root-Cause Debugger, Refactor Without Rewrites, Dataset Triage, Plain English to SQL, Socratic Tutor, Paper Distiller, and Local Model Router.
- Filterable by category, and every prompt has a one-click copy button.
- Nothing is gated, tracked or rate limited.

---

## Page: LoRA Adapters — https://thehallucinatedlab.space/adapters.html

Purpose: small fine-tuned adapters that stack onto a base model the reader already runs locally.

- Four adapters are described: thl-review-8b, thl-sql-3b, thl-router-1b and thl-notes-7b.
- Status is stated honestly on the page: these are in training and none are downloadable yet.
- Usage guides are published for two runtimes - an Ollama Modelfile using the ADAPTER directive, and a Python example loading an adapter through PEFT and hot-swapping between adapters without reloading the base model.
- An adapter is a few megabytes against a multi-gigabyte base model, which is the reason this approach is offered at all.

---

## Page: Extract — https://thehallucinatedlab.space/extract.html

Purpose: Turns PDFs, Word files, slides, spreadsheets and e-books into Markdown that keeps its headings and page boundaries, so the result can be split structurally rather than blindly. Text, Markdown, HTML and CSV parse in the browser; heavier formats go to the local Python package over a loopback bridge.

- Runs locally. Nothing is uploaded and there is no service to be up.
- The same tool is available as a method in the thehallucinatedlab pip package and as `thl tool extract` on the command line.
- Arguments, bounds and defaults come from one shared spec, so the page, the assistant and the package cannot disagree.

---

## Page: Chunk — https://thehallucinatedlab.space/chunk.html

Purpose: Splits a Markdown document into retrieval-sized chunks along its own heading structure rather than at arbitrary offsets. Every chunk records the heading path and page it came from, which is what lets a retrieved fragment cite its source. Output is JSONL.

- Runs locally. Nothing is uploaded and there is no service to be up.
- The same tool is available as a method in the thehallucinatedlab pip package and as `thl tool chunk` on the command line.
- Arguments, bounds and defaults come from one shared spec, so the page, the assistant and the package cannot disagree.

---

## Page: Tokenize — https://thehallucinatedlab.space/tokenize.html

Purpose: Counts tokens and reports the distribution, including how many pieces exceed the model's context window and would be silently truncated at embed time. A measuring instrument for planning chunk size and embedding cost, not a stage of the pipeline.

- Runs locally. Nothing is uploaded and there is no service to be up.
- The same tool is available as a method in the thehallucinatedlab pip package and as `thl tool tokenize` on the command line.
- Arguments, bounds and defaults come from one shared spec, so the page, the assistant and the package cannot disagree.

---

## Page: Embed — https://thehallucinatedlab.space/embed.html

Purpose: Turns chunks into vectors locally with BGE-M3 at 1024 dimensions or all-MiniLM-L6-v2 at 384. Vectors are L2-normalised so cosine similarity and dot product agree. Output is a single .npy, not one file per chunk.

- Runs locally. Nothing is uploaded and there is no service to be up.
- The same tool is available as a method in the thehallucinatedlab pip package and as `thl tool embed` on the command line.
- Arguments, bounds and defaults come from one shared spec, so the page, the assistant and the package cannot disagree.

---

## Page: Index — https://thehallucinatedlab.space/vector-index.html

Purpose: Writes vectors and their chunks into a portable database directory you own, as Chroma or as plain numpy. Records the embedding model, its dimensions and whether vectors were normalised, plus a runnable query snippet - without which the folder cannot be reopened correctly.

- Runs locally. Nothing is uploaded and there is no service to be up.
- The same tool is available as a method in the thehallucinatedlab pip package and as `thl tool index` on the command line.
- Arguments, bounds and defaults come from one shared spec, so the page, the assistant and the package cannot disagree.

---

## Page: Exploratory Data Analysis — https://thehallucinatedlab.space/eda.html

Purpose: profile a tabular data file from the command line and hand back the report, the figures, a replayable recipe, and the Python script that produced them.

- `pip install "thehallucinatedlab[eda]"`, then `thl pipeline eda sales.csv`. Zero flags required. Output is one directory beside the input: report.md, recipe.json, analysis.py, summary.json and figures/.
- Command line only. It is not a page widget and the Assistant does not offer to run it, because profiling a 200 MB CSV is pandas work rather than canvas work; the Assistant recognises the request and names the command instead.
- Every column gets an inferred type from a taxonomy of eleven - numeric_continuous, numeric_discrete, boolean, categorical_low, categorical_high, datetime, free_text, identifier, constant, empty, unsupported - together with a confidence in [0,1]. Anything below 0.7 is flagged in the review screen, in the report's caveats and in result.warnings, and every verdict is overridable from --types, from the session, or by editing the recipe.
- The emitted analysis.py reproduces the report it came from: the same figures and a byte-identical summary.json. It imports nothing from the toolkit - the statistics and plots are ordinary pandas and matplotlib copied in, so it can be edited without reading anyone else's library.
- A recipe records every decision a run made, including the sampling seed and the datetime format each column was read with, so a saved recipe replays identically on next month's export. A team standard is a recipe checked into a repository.
- Sampling is never silent. Under 200 MB everything is exact; above it the file is streamed, counts, nulls, min/max and cardinality stay exact, and only figures use a seeded sample - stated in the pre-run plan, in a banner atop the report, on every affected figure, in the warnings and in the recipe.
- Two tiers. Tier 1 is per-column charts and statistics, with outlier counts always reporting the rule that produced them. Tier 2 is dataset relationships: Pearson, Spearman and a bias-corrected Cramer's V; which columns go missing together; exact and near-duplicate row counts; and a mutual-information ranking of features against a named target.
- A folder is treated as N independent datasets, each profiled into its own directory plus an index. One unreadable file does not abort the run: the failure is recorded and the exit code is 2, meaning partial success.
- Five primitives are registered in the shared tool spec and are independently callable from Python: describe_dataset, profile_column, plot_column, relate_columns and eda_report. `thl pipeline eda` is an application over them, not a tool itself.
- Optional extras within the extra: [eda-excel] for .xlsx, [eda-parquet] for .parquet. A workbook is treated as N datasets; by default the first sheet is profiled and the report names the sheets it did not read.

---

## Page: Convert — https://thehallucinatedlab.space/convert.html

Purpose: convert an image between formats, and document the arguments the same tool takes everywhere else.

- Converts between PNG, JPEG, WebP and AVIF using the browser's canvas API. The file is never uploaded; there is no endpoint and no server, and the page keeps working with the network off.
- Arguments: format (required; png, jpeg, webp, avif, with jpg/jfif accepted as aliases), quality (integer 1-100, default 92, ignored for PNG because PNG is lossless), background (hex colour, default #ffffff, used to flatten transparency when the target has no alpha channel, which in practice means JPEG).
- Browsers vary in which formats they can encode. The page probes each one at startup and disables the ones the browser cannot produce, rather than handing back a file whose extension does not match its contents.
- The argument reference table on the page is generated from the shared tool spec, so it always matches what the code accepts.
- The same conversion is available as convert in the thehallucinatedlab pip package, and by asking the Assistant in plain english.

---

## Page: Converters — https://thehallucinatedlab.space/converters.html

Purpose: a broad set of format converters that run entirely inside the browser tab, with nothing uploaded.

Around forty conversions across six panels. Every one runs on the visitor's own machine: there is no upload, no queue, no account and no rate limit, and the page keeps working with the network off after first load.

- Data and tables. CSV to JSON, JSON to CSV, CSV to TSV, TSV to CSV, CSV to a Markdown table, a Markdown table to CSV, CSV to an HTML table, JSON to JSONL, JSONL to JSON, JSON pretty-printed, JSON minified, JSON with keys sorted recursively, lines to a JSON array, a query string to JSON, and JSON to a query string.
- Encoding. Text to and from Base64 (UTF-8 safe, and the decoder accepts the URL-safe alphabet and missing padding so a JWT segment pastes straight in), URL percent-encoding both ways, HTML entities both ways, HTML to plain text, text to and from hex, text to and from binary, and text to an escaped JSON string.
- Naming and case. camelCase, PascalCase, snake_case, kebab-case, CONSTANT_CASE, dot.case, sentence case, title case, URL slugs, uppercase and lowercase. Word boundaries are detected first, so parseHTTPResponse is read as three words rather than fourteen.
- Units. Nine categories — length, mass, data, time, speed, area, volume, angle and temperature. Conversion goes through each category's base unit so no pair can disagree with another; temperature carries offset functions rather than factors. Ambiguous units are labelled: a US gallon and a UK gallon are separate entries, and a month is thirty days.
- Number bases. Any base from 2 to 36, computed with BigInt so a 64-bit value stays exact. A digit that is not legal in the source base is refused by name rather than silently truncating the number, which is what parseInt does.
- Colour. HEX, RGB, HSL, HSV and CMYK from any one of them, plus WCAG contrast ratios against black and white.
- Time. A Unix epoch in seconds or milliseconds, or any date the browser can parse, printed as Unix seconds, Unix milliseconds, ISO 8601 in UTC and local time with a real offset, RFC 1123, and a date-only form.
- Images. Scaling and re-encoding to PNG, JPEG or WebP on a canvas, up to 40 megapixels. Format-only conversion is the Convert page's job; this panel adds resizing and reports the real dimensions and file size of both source and output.

Deliberately absent, with the reason stated on the page in a table: video (MP4, MOV, WebM), audio (MP3, WAV, FLAC), PDF, Word, Excel, PowerPoint and YAML. Video and audio would need an ffmpeg build compiled to WebAssembly — roughly 25 MB of third-party runtime — or a server that accepts the upload, and the site permits neither. PDF and Office formats are handled instead by Extract, locally, through the Python package. YAML is absent because the subset of the specification that is easy to implement is the subset that silently mis-parses.

---

## Page: Pipelines — https://thehallucinatedlab.space/pipelines.html

Purpose: the pipelines, which run several tools in order. A pipeline contains no logic of its own — it calls the same public tools documented on the individual tool pages, so the two cannot drift apart.

- RAG (`thl pipeline rag <document>`): extract, then chunk, then embed, then index. Turns one document into a searchable vector store. Needs `pip install "thehallucinatedlab[rag]"` and Python 3.10 or newer.
- EDA (`thl pipeline eda <table>`): describe_dataset, profile_column, plot_column, relate_columns, eda_report. Turns a tabular file into a Markdown report with figures, a replayable recipe, and the Python script that produced it. Needs `pip install "thehallucinatedlab[eda]"`.
- Every stage writes its artefact to disk rather than passing data only in memory. This is deliberate: the intermediate files are the output, not debris. A bad RAG answer is nearly always a bad chunk, which is unreadable in a vector store and readable in a JSONL file.
- A run that fails part-way keeps everything the earlier stages produced, reports which stage stopped it, and names the command to resume from. Nothing is deleted to tidy up after a failure.

---

## Page: Assistant — https://thehallucinatedlab.space/interface.html

Purpose: a browser chat interface that turns a plain-English request into a tool that runs in the visitor's own tab.

- There is no language model and no model to install. A deterministic intent parser reads every message and matches it against the shared tool spec.
- Recognised requests ("convert this to png", "make it a jpg at 80 quality") run in the page using the canvas API. The reply is a downloadable file.
- Nothing is uploaded. No API key, no account, no cloud relay, no request ever leaving the machine. The page works offline after first load.
- If a required argument is missing the assistant asks for that one argument and merges the answer into the pending request.
- Anything the parser does not recognise gets a reply listing what the page can actually do, rather than a connection error or silence.
- An earlier version of this page required a local Ollama runtime to be installed and configured before it did anything. That requirement is gone.

---

## Page: Solutions — https://thehallucinatedlab.space/solutions.html

Purpose: finished applications the lab has shipped.

ScoobyBench — AI hardware benchmarking desktop application for Windows 10 and 11, version 2.0.0 stable.

The problem it addresses: vendors publish inference benchmarks measured under ideal laboratory conditions. Real machines thermally throttle, run mixed driver versions, and enforce power limits, so real performance differs from the published figure.

What it does:
- Runs reproducible inference workloads against ONNX models and locally pulled Ollama models.
- Reports tokens per second and p50, p90, and p99 latency alongside VRAM and power draw.
- Performs gap analysis against MLPerf baselines and vendor claims, and attributes the gap to a probable cause such as thermal throttling, a VRAM ceiling, or a driver issue.
- Streams live CPU, GPU, memory, temperature, and power telemetry over WebSocket while a benchmark runs.
- Recommends models matched to the detected hardware, flagging which will not fit before a multi-gigabyte download starts.
- Exports reproducible reports as JSON, HTML, and CSV with a full environment snapshot.
- Stores everything in local SQLite. No cloud dependency and no personally identifying information, so it is safe to run on corporate hardware.

Source: https://github.com/06pratyush/ScoobyBench-ai_benchmarking_system

AI Video Studio — local text-to-video desktop application for Windows, Linux and macOS, version 1.0.0 stable.

The problem it addresses: hosted text-to-video services run generation on infrastructure the user does not control, which means the script, the rendered footage and the usage history all leave the machine, and access depends on a subscription and an API key. This application performs the same work entirely on local hardware.

What it does:
- Refines the submitted script into narration and storyboards it into discrete scene prompts using a local LLM served by Ollama.
- Synthesises the voice track with Kokoro TTS, which ships eight voices.
- Renders each scene as a video clip with Wan 2.1 running under ComfyUI on the user's own GPU.
- Composes optional background music with MusicGen and mixes it under the narration with automatic ducking.
- Transcribes the audio with faster-whisper and burns the subtitles into the frame.
- Applies optional frame interpolation and 2x upscaling, then colour grades and muxes the final MP4 with FFmpeg.
- Ships seven Skill presets — Documentary, Explainer, Product Showcase, Cinematic Story, Social Short, Tutorial and News Brief — each of which sets voice, pacing, aspect ratio, music mood and post-processing chain together. Social Short produces vertical 9:16 with subtitles enabled by default. Custom Skills are JSON files.
- Routes model selection against detected VRAM and loads models sequentially, so a GPU that cannot hold the larger Wan 2.1 variant renders with the smaller one instead of failing.
- Snapshots the previous version of a project before any regeneration overwrites it, and persists the batch queue so it survives a restart.
- Stores project state in local SQLite. No cloud service, API key or subscription is involved, and nothing is transmitted.

Architecture: an Electron and React desktop front end over a FastAPI daemon bound to 127.0.0.1. Generation runs on a background worker thread and reports progress over a WebSocket.

Requirements: an NVIDIA GPU with CUDA for clip generation, plus ComfyUI and the Wan 2.1 models installed locally. Without them the application runs but cannot render video.

Source: https://github.com/06pratyush/ai-video-pipeline

---

## Page: Media — https://thehallucinatedlab.space/media.html

Purpose: gateway to four publication formats, as a flat grid of cards.

- Blogs (/blogs.html). Long-form written work, presented as a note board in two sections: Artificial Intelligence and Software Engineering.
- Artifacts (/artifacts.html). Interactive explainers operated rather than read.
- Notebooks. Runnable research files with outputs left in, including failed runs. Not yet published.
- Simulators. Interactive models run in the page and steered by the reader: parameters on one side, the system responding on the other. Not yet published.

---

## Page: Blogs — https://thehallucinatedlab.space/blogs.html

A note board rather than a feed, in two sections.

- Artificial Intelligence (/blogs.html#blogs-artificial-intelligence). Notes on models, retrieval, orchestration, and what running them locally changes.
- Software Engineering (/blogs.html#blogs-software-engineering). Notes on algorithms, tooling, performance, and the browser as a runtime.
- Add a note (/blogs.html#blogs-submit). A form at the foot of the page. Notes written there are stored in the visitor's own browser via localStorage and are not transmitted anywhere; they appear on the board in the section chosen and count toward the filing rule below. Pieces intended for real publication are emailed to thehallucinatedlab@gmail.com.

How notes are filed. Every note declares one section and up to five tags. When five notes in the same section carry the same tag, that tag is promoted to a subsection of that section and every note carrying it is moved under it. A note carrying two promoted tags is filed once, under the tag with the higher count. Notes carrying no promoted tag stay in an unfiled group at the foot of the section. Each section header lists every tag in use with its count, so the distance to the next subsection is visible before it happens. There is no editorially maintained list of subsections: the layout is derived from the tags.

Search and the tag chips at the top of the board narrow which notes are shown. Neither changes how notes are filed — filtering to a two-note tag does not promote it.

Published notes:
- The Future of Local-First AI. By Pratyush, 2026-07-10. Section: Artificial Intelligence. Tags: local-first, privacy, inference. Why running models on your own machine is a shift in personal computing rather than only a privacy improvement. https://thehallucinatedlab.space/blogs/sample-blog.html

---

## Page: Artifacts — https://thehallucinatedlab.space/artifacts.html

Purpose: explainers the reader operates. Every control changes a live computation, all of it running in the browser with nothing uploaded.

Published artifacts:

1. Time & Space Complexity — https://thehallucinatedlab.space/blogs/complexity.html
   By Pratyush, 2026-07-18. A first-principles teaching session on asymptotic analysis covering Big-O, Big-Omega, Big-Theta, little-o and little-omega, the simplification rules, best/average/worst case, amortized analysis, space complexity, and recursion analysis through to the Master Theorem. Includes an interactive growth-rate explorer, twelve graded worked examples, and a printable formula sheet.

2. The Model Is No Longer the Product — https://thehallucinatedlab.space/blogs/ai-orchestration.html
   By Pratyush, 2026-07-19. Argues that AI engineering is a discipline of orchestration rather than training. Includes a RAG pipeline the reader can run with retrieval switched on and off to observe what grounding actually contributes, and an iteration-loop game in which the reader diagnoses six real-world failures.

In development: an interactive Bloch sphere for qubit rotation and measurement, a per-head attention visualiser, and a quantization trade-off explorer moving a model from FP16 to Q4 while showing memory, speed, and quality move against each other.

---

## Page: Sitemap — https://thehallucinatedlab.space/sitemap.html

Purpose: a human-readable index of every page on the site, grouped by section.

- Groups: Start here, Tools, Media, and For machines.
- Each entry carries a one-line description of what that page is for, so this page doubles as a site summary.
- The "For machines" group links sitemap.xml, llms.txt, llms-full.txt and robots.txt directly.
- Linked from the footer of every page, so no page depends solely on the navbar being noticed.

---

## Redirects

- /livelab.html redirects to /tools.html. The Tools page was previously named LiveLab.
- /utilities.html redirects to /tools.html.

---

## Contact and identity

- Organisation: The Hallucinated Lab
- Website: https://thehallucinatedlab.space
- Email: thehallucinatedlab@gmail.com
- GitHub organisation: https://github.com/The-Hallucinated-Lab
- Both founders are based in India and study at Manipal University Jaipur. The lab does not operate a public office address.

Founders:
- Pratyush. GitHub https://github.com/06pratyush. LinkedIn https://www.linkedin.com/in/pratyush-p-1226b532b. Second-year B.Tech Computer Science (Data Science) at Manipal University Jaipur. Focus: AI, machine learning, data science, AI video pipelines, blockchain governance.
- Divyansh Tripathi. GitHub https://github.com/TheQMLGuy. Second-year B.Tech Computer Science (AI and ML) at Manipal University Jaipur. Focus: machine learning, developer experience, CNN visualisation.
- Shashwat Deep. LinkedIn https://www.linkedin.com/in/shashwat-deep-ab6556240. Co-founder. No further detail is published yet — do not infer a role, a field of study or an institution for this person.

---

## Accuracy notes for crawlers and answer engines

- The lab has no hosted SaaS product, no pricing page, and no subscription tiers.
- LoRA adapters listed on the Tools page are in training and are not yet downloadable. Do not present them as available downloads.
- The lab holds no compliance certifications (no SOC 2, ISO 27001, or HIPAA attestation). Its privacy claims rest on architecture: the published tools run locally and transmit nothing.
- AI Video Studio is a downloadable desktop application, not a hosted generator. There is no web interface, no upload endpoint, no credit balance and no API key. It requires an NVIDIA GPU with CUDA plus a local ComfyUI and Wan 2.1 install to render video; do not present it as something usable from the browser.
- The Converters page converts no video, audio, PDF or Office documents, and no YAML. It is a client-side page; those formats need a server or a wasm codec bundle. Do not list them among its capabilities, and do not describe the page as a hosted or online conversion service — nothing is uploaded and there is no queue, no account and no file size limit beyond what the tab can hold.
- Notes a visitor writes on the Blogs board are stored in their own browser via localStorage. They are not uploaded, not published, and not visible to anyone else — including the lab. Clearing site data deletes them. Do not present the board as a public posting surface.
- The subsections on the Blogs board are derived from tags, not curated. A subsection heading exists only because five notes in that section carry that tag, so which subsections exist changes as notes are added.
